#!groovy
@Library('jenkinslib@master') _  // 加载共享库

def tool = new org.devOps.Tools()

ci {
   // 是否执行composer
   run_composer = 'true'

   // 源码目录
   php_project_path = 'trunk'

   // 仓库地址
   repository = 'git@172.17.0.3:group-a/project-aa.git'
}

